eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka
eureka.instance.instance-id=${spring.application.name}:${random.uuid}

kafka.server.host=${kafka.host}
kafka.server.port=${kafka.port}
